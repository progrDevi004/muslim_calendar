name: muslim_calendar
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  syncfusion_flutter_calendar: ^28.1.33
  shared_preferences: ^2.3.3
  flutter_colorpicker: ^1.0.3
  sqflite: ^2.0.0+4
  path: ^1.8.0
  provider: ^6.0.0
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/country_city_data.json
